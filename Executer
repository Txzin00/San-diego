-- ==========================================
-- SAN DIEGO HUB - CORRIGIDO
-- ==========================================

local Players = game:Service("Players")
local TweenService = game:Service("TweenService")
local UserInputService = game:Service("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- Garante que o PlayerGui existe antes de criar a interface
local playerGui = LocalPlayer:WaitForChild("PlayerGui", 15)
if not playerGui then 
    warn("Não foi possível carregar a PlayerGui a tempo.")
    return 
end

-- Deleta o menu antigo se ele já existir para não duplicar
if playerGui:FindFirstChild("SanDiegoHub") then
    playerGui.SanDiegoHub:Destroy()
end

-- Criando a Tela
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SanDiegoHub"
ScreenGui.Parent = playerGui
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- [O restante do código do Auto Farm continua exatamente igual]
